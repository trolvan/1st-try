export const introduceText: string = '首先这是一段介绍文字。\n然后这是一段用来介绍这个页面是用来干什么的文字。'
export const toolList: object[] = Array(12).fill({}).map((t, i) => ({ name: `demo工具-${i + 1}`, scon: 'tool', path: '/tools/fontCutter' }))
